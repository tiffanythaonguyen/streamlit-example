git add app.py  # Replace 'app.py' with the correct filename if it's different.
git commit -m "Add app.py"  # Add a meaningful commit message.
git push origin main  # Push the changes to your GitHub repository's main branch.
